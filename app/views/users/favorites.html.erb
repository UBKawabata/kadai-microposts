<div class="row">
  <aside class="col-xs-4">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title"><%= @user.name %></h3>
      </div>
      <div class="panel-body">
        <img class="media-object img-rounded img-responsive" src="<%= gravatar_url(@user, { size: 500 }) %>" alt="">
      </div>
    </div>
    <%= render 'favorites/favorite_button', micropost: @micropost %>
  </aside>
  <div class="col-xs-8">
    <ul class="nav nav-tabs nav-justified">
      <li class="<%= 'active' if current_page?(micropost_path(@micropost)) %>"><%= link_to micropost_path(@micropost) do %>Microposts <span class="badge"><%= @count_microposts %></span><% end %></li>
      <li class="<%= 'active' if current_page?(favorites_micropost_path(@micropost)) %>"><%= link_to favorites_micropost_path(@micropost) do %>Favorites <span class="badge"><%= @count_favorites %></span><% end %></li>
    </ul>
    <%= render 'micropost', micropost: @favorites %>
  </div>
</div>